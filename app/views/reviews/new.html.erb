<% @flat = Flat.find(@booking.flat_id) %>
<h1>How was your stay at <strong class="text-muted"><%= @flat.address %></strong>?</h1>
<div class="container">
  <%= simple_form_for [ @booking, @review ] do |f| %>
    <%= f.input :rating, collection: [1, 2, 3, 4, 5] %>
    <%= f.input :content, hint: "Tell us more about your experience in #{@flat.address}" %>
    <%= f.submit "Add review", class: "btn btn-primary" %>
  <% end %>
</div>
